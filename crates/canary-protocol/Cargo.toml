[package]
name = "canary-protocol"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
canary-models = { path = "../canary-models" }
canary-data = { path = "../canary-data" }
chrono = { workspace = true }
